# PRD · 需求契约

> 这是「产品经理」与用户讨论打磨的产物，会随对话演化。由用户确认后才定稿。
> 与 `AGENTS.md`（不变项）互补：本文件管"需求与走向"，`AGENTS.md` 只存不会变的东西。
> 本文件属于某一轮任务，**放在 `docs/runs/NNN-<功能名>/` 里**，项目根的 `PRD.md` 是指向它的指针。

## 所属轮次
- 轮次：NNN-<功能名>
- 开轮日期：YYYY-MM-DD

## 一句话定位
___

## 解决的问题
___

## 目标用户
___

## 核心价值
___

## 功能范围
- ___
- ___

## 第一个检查点（最小可演示里程碑）
___

## UI/UX 设计决策（前端项目才填，无前端写"无"）
> 由 pm 讨论阶段的用户偏好 + 设计链路（engineer 自动判级：ui-ux-pro-max 检索 / Webdesign 规范 / 需要时 huashu 高保真，用户确认）共同填写。确认后是既定基线，界面实现必须遵守。

- 设计级别（L1 组件小改 / L2 新页面大改）：___
- 视觉风格：___
- 配色 / 字体：___
- 设计 token 出处：___
- 参考产品/感觉：___

## 需求批判（定稿前 RedTeam 轻量批判，v4）
> pm 提交确认前调 RedTeam 产出「3 个最强反驳 + 推荐」；小任务可写"已跳过"。RedTeam 缺失时写 pm 自查三问结论。

- 最强反驳 1：___
- 最强反驳 2：___
- 最强反驳 3：___
- 推荐 / 结论：___

## 研究结论清单（engineer 开工必读）
> 研究产生的结论落盘在此引用，engineer 实现前**必须**先读这些文件；实现中新增研究也在此登记。这治"探索完就断"。

| 研究主题 | 结论文件 | 一句话结论 |
| --- | --- | --- |
| _例：认证库选型_ | `docs/research/auth-lib.md` | **用 X 库**（理由：…） |

## 确认状态
> 用户对该契约的确认：**待确认 / 已确认（日期）**
